import React from 'react';
import Spinner from '../../Components/Spinner/Spinner';

const Home = () => {
    return (
        <div className=''>
            <Spinner></Spinner>
            
        </div>
    );
};

export default Home;